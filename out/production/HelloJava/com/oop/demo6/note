super注意点：
    1、super调用父类的构造方法，必须在构造方法的第一个
    2、super必须只能出现在子类的方法或者构造方法中
    3. super和this不能同时调用构造方法！

vs this
    代表的对象不同：
        this：本身调用者这个对象
        super: 代表父类对象的应用
    前提:
        this: 没继承也可以使用
        super：只能在继承条件下才可以使用
    构造方法：
        this() 本类的构造
        super() 父类的构造


为什么要重写？
1、父类的功能，子类不一定需要，或者不一定满足
command + n; override


重写：需要要继承关系，
1、方法名必须相同
2、参数列表必须相同
3、修饰符：范围可以扩大，但不能缩小  public > protected > default > private
4、抛出的异常: 范围可以所辖，但不能扩大  classNotFoundException --> Exception

重写：父类的方法和子类必须要一致，方法体不同
